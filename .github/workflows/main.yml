name: Unity CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4

      - uses: game-ci/unity-actions/activate@main

      - name: Build
        uses: game-ci/unity-actions/build@main
